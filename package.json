{
  "name": "try",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Try — personalised triathlon training plans (swim, bike, run).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "lint:boundaries": "node scripts/check-boundaries.mjs",
    "check": "npm run lint:boundaries && npm run test && npm run build"
  },
  "dependencies": {
    "@clerk/react": "^6.11.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.7.0",
    "vite": "^6.4.3",
    "vite-plugin-pwa": "^0.21.2",
    "vitest": "^4.1.9"
  }
}
